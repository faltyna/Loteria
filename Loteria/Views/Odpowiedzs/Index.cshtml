@model IEnumerable<Loteria.Models.DbModels.Odpowiedz>

@{
    ViewBag.Title = "Index";
}

<style>
    h2 {
        color: darkblue;
        font-family: monospace;
    }

    .class {
        background-color:aliceblue;
    }
</style>

<h2>Odpowiedź</h2>

<p>
    @Html.ActionLink("Dodaj odpowiedź", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pytanie.tresc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tresc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.poprawnoscOdpowiedzi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pytanie.tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.poprawnoscOdpowiedzi)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.odpowiedzId }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.odpowiedzId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.odpowiedzId })
        </td>
    </tr>
}

</table>
